{
  "name": "astrid-sdk-js-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "JavaScript / TypeScript SDK for Astrid OS capsules. Workspace root.",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
